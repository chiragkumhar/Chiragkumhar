//Program-8. Write a program to find out the max from given number (E.g., No: -1562 Max number is 6)

#include<stdio.h>
main()
{
	int i,num,max;
	printf("Enter the Numbers : ");
	scanf("%d",&num);
	
	for(i=num;i<=max;i++)
	{
		if(i<=max);
		
	}
	printf("Max of Given Number is %d",max);
	
}
